<h1>診断結果を見る</h1>
 <% @career_anchor.each do |career_anchor|%>
  <li>
     <%= l career_anchor.created_at %>
     <%= link_to "診断結果", career_anchor_path(career_anchor.id), method: :get %>
  </li>
 <% end %>